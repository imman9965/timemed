import 'package:flutter/material.dart';

class PharmacyLoginPage extends StatelessWidget {
  const PharmacyLoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Pharmacy Login"), centerTitle: true),
    );
  }
}
